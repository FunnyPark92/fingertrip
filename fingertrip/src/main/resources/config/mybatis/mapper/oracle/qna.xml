<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="config.mybatis.mapper.oracle.qna">
	<select id="selectAll" parameterType="SearchVo" resultType="QnAVo">
	 	select *
		from (
			select rownum as RNUM, list.*
			from(
				select * from qna 
				<if test="searchKeyword!=null and searchKeyword!=''">
					where ${searchCondition} like '%' || #{searchKeyword} || '%'
				</if>
				order by group_no desc, sort_no
				)list
			)
		<![CDATA[
		where RNUM>#{firstRecordIndex} 
		and RNUM<=#{firstRecordIndex}+#{recordCountPerPage}
		]]>
	</select>
	
	<select id="selectAllNotice" parameterType="SearchVo" resultType="NoticeVo">
		select *
		from (
			select list.*, rownum as RNUM
			from(
				select * from notice 
				<if test="searchKeyword!=null and searchKeyword!=''">
					where ${searchCondition} like '%' || #{searchKeyword} || '%'
				</if>
				order by notice_no desc
				)list
			)
		<![CDATA[
		where RNUM>#{firstRecordIndex} and RNUM<=#{firstRecordIndex}+#{recordCountPerPage}
		]]>
	</select>
	
	<select id="totalRecord" resultType="int" parameterType="SearchVo">
		select count(*)
		from notice
		<if test="searchKeyword!=null and searchKeyword!=''">
			where ${searchCondition} like '%' || #{searchKeyword} || '%'
		</if>
	</select>
	
	<update id="countUpdate" parameterType="int">
		update qna
		set read_count=read_count+1
		where qna_no=#{qnaNo}
	</update>
	
	<update id="countUpdateNotice" parameterType="int">
		update notice 
		set read_count=read_count+1
		where notice_no=#{noticeNo}
	</update>
	
	<select id="selectByNo" parameterType="int" resultType="QnAVO">
		select * from qna
		where (group_no=#{groupNo}+1 or group_no=#{groupNo}-1) and step=0
		order by qna_no
	</select>
	
	<select id="passck" parameterType="int" resultType="string">
		select password from qna
		where qna_no=#{qnaNo}
	</select>
	
	<select id="QnAtotalRecord" parameterType="SearchVo" resultType="int">
		select count(*) from qna
	<if test="searchKeyword!=null and searchKeyword!=''">
		where ${searchCondition} like '%' || #{searchKeyword} || '%'
	</if>
	</select>
	
	<select id="selectByNoOne" parameterType="int" resultType="QnAVO" >
		select* from qna
		where qna_no=#{qnaNo}
	</select>
	
	<update id="updateSortNo" parameterType="int">
		update qna
		set sort_no=sort_no+1
		where group_no=(select group_no from qna where qna_no=#{qnaNo}) and  sort_no>(select sort_no from qna where qna_no=#{qnaNo})
	</update>
	
	<insert id="reply" parameterType="QnAVO">
		insert into qna(qna_no,title,content,name,password,group_no,step,sort_no,travel_agency_no,admin_no,member_no)
		values(qna_seq.nextval,#{title},#{content},#{name},#{password},(select group_no from qna where qna_no=#{qnaNo}),
		(select step from qna where qna_no=#{qnaNo})+1,
		(select sort_no from qna where qna_no=#{qnaNo})+1,#{travelAgencyNo},#{adminNo},#{memberNo})
	</insert>
		
	<select id="noticeDetail" parameterType="int" resultType="noticeVo">
		select * from notice
		where notice_no=#{noticeNo}
	</select>
	
	<select id="getAdminNo" parameterType="string" resultType="int">
		select admin_no from admin where id=#{id}
	</select>
	
	<insert id="noticeInsert" parameterType="noticeVo">
		insert into notice(notice_no,title,content,admin_no )
		values(notice_seq.nextval, #{title},#{content},#{adminNo})
	</insert>
	
	<select id="serchGroupNo" parameterType="int" resultType="int">
		select group_no from qna
		where qna_no=#{qnaNo}
	</select>
	
	<select id="myWriteSelectAll" parameterType="SearchVo" resultType="QnAVo">
		select * from(
			select list.*, rownum as RNUM from(
				select * from qna
				where member_no=#{memberNo}
				order by qna_no desc
			)list
		)
		<![CDATA[
		where RNUM>#{firstRecordIndex} and RNUM<=#{firstRecordIndex}+#{recordCountPerPage}
		]]>
	</select>
	
	<select id="myWriteQnAtotalRecord" parameterType="SearchVo" resultType="int">
		select count(*) from qna
		where member_no=#{memberNo}
	</select>
</mapper>









