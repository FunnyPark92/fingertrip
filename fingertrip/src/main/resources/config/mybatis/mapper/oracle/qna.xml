<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="config.mybatis.mapper.oracle.qna">
	<select id="selectAll" resultType="QnAVO">
		select * from qna
		order by qna_no
	</select>
	
	<select id="selectAllBoard" resultType="map">
		select n.*, a.name
		from notice n join admin a
		on n.admin_no=a.admin_no
	</select>
	
	<update id="countUpdate" parameterType="int">
		update qna
		set read_count=read_count+1
		where qna_no=#{qnaNo}
	</update>
	
	<update id="countUpdateboard" parameterType="int">
		update notice 
		set read_count=read_count+1
		where notice_no=#{boardNo}
	</update>
	
	<select id="selectByNo" parameterType="int" resultType="QnAVO">
		select * from qna
		where qna_no=#{qnaNo}
	</select>
</mapper>









